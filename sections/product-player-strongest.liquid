
<div class="unbelievable_sound_padding">
    <div class="unbelievable_sound_box magnet_color">
 
      	<div class="unbelievable_sound_right">
        	{% if section.settings.speaker_image != blank %}
                <img src="{{ section.settings.speaker_image | img_url: 'master' }}">
            {% endif %}
        </div>

        <div class="unbelievable_sound_left">
          	
        	{% if section.settings.heading != blank %}
            	<h3>{{ section.settings.heading }}</h3>
            {% endif %}
          	{% if section.settings.sub_heading != blank %}
            	<h2>{{ section.settings.sub_heading }}</h2>
            {% endif %}
			{% if section.settings.text != blank %}
            	<p>{{ section.settings.text }}</p>
            {% endif %}
        	
          	{%- if section.settings.link_text != blank -%}
             <div class="button">
               <a href="{{ section.settings.link }}" class="addcart">
                 {{ section.settings.link_text }}
               </a>
             </div>
            {%- endif -%}
        </div>
    </div>
</div>


{% schema %}
  {
    "name": "Player strongest",
    "settings": [
	  {
        "type": "text",
        "id": "heading",
        "label": "Heading"
      },
	  {
        "type": "text",
        "id": "sub_heading",
        "label": "Sub heading"
      },
	  {
        "type": "textarea",
        "id": "text",
        "label": "Text"
      },
	  {
        "type": "image_picker",
        "id": "speaker_image",
        "label": "speaker Image"
      },
	  {
          "type": "url",
          "id": "link",
          "label": "button link"
        },
        {
          "type": "text",
          "id": "link_text",
          "label": "Button text",
          "default": "Optional button"
        }
    ],    
    "presets": [
      {
        "name": "Player strongest",
        "category": "Image"
      }
    ]
  }
{% endschema %}

